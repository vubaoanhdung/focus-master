import { Link } from "react-router-dom";

const Brand = () => {
    return (
        <Link to="/" className="navbar-brand text-white-50">
            FocusMaster
        </Link>
    );
};

export default Brand;
